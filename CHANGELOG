0.6

* Add methods all_standard_errors and all_parameter_estimates on an SO to gather values across SOBlocks
* Fix bug stopping IndivContribToLL from being loaded
* Add path.expand() to file in so_SO_read() call in R
* Minor speedup in loading data.frames into R
