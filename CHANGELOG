0.6

* Add methods all_standard_errors and all_parameter_estimates on an SO to gather values across SOBlocks
* Add method all_simulated_profiles on an SOBlock to gather values across SimulationBlocks
* Fix bug stopping IndivContribToLL from being loaded
* Add path.expand() to file in so_SO_read() call in R
* Minor speedup in loading data.frames into R
* columnTypes are allowed to be a space separated array of types
* Add functions dv_column, dv_column_name, idv_column, idv_column_name, id_column and id_column_name to lookup indices and names for these columns
* Add functions so_SO_is_ruv_parameter and so_SO_is_structural_parameter to get information from PharmML
* Add R SO method variability_type to list variability types of parameters
